{
  "name": "twilio-calls",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal backend for turn-based translated PSTN calls using Twilio and Azure",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "twilio": "^5.13.1"
  }
}
